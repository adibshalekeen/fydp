{
  "name": "server",
  "version": "1.0.0",
  "description": "Receives API requests to control LED connected to GPIO pins (hopefully)",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "2019.037",
  "license": "ISC"
}
